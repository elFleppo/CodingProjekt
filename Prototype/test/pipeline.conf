input {
  file {
    id => "dblp.json"
    path => "/tmp/dblp.json"
    codec => "json"

  }
}
filter{
  json {
    source => "message"
  }
}
output {
   elasticsearch {
     ssl => "false"
     hosts => ["es01:9200"]
     index => "index"
   }
}
